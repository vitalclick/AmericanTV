// Placeholder for the default Flutter widget test.
//
// This file exists to suppress the `flutter create` step from
// generating its built-in `MyApp`-referencing scaffold, which doesn't
// compile in this project (entrypoint is `App`, not `MyApp`). Real
// widget tests live alongside their features under test/features/.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — feature widget tests live under test/features/', () {
    expect(true, isTrue);
  });
}
