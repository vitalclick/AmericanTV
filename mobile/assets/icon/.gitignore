# From 3.0.0+14 the production masters are committed so a fresh
# clone + Codemagic build doesn't depend on env-var-shipped binaries.
# Any OTHER PNG (e.g. work-in-progress alternatives, design exports)
# stays ignored.
*.png
!app-icon.png
!app-icon-adaptive-foreground.png
